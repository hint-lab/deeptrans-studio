# 更新日志

DeepTrans Studio 的重要变更记录在此文件中。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)，分类方式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

> 版本说明：仓库此前从初始提交起一直保留 `0.1.0`，且没有 Git Tag。`0.6.0` 是开始正式、可追溯版本管理后的首个版本；为避免虚构历史，不补写不存在的 `0.2.0`–`0.5.x` 发布记录。

## [未发布]

### 新增

- Tauri 桌面客户端交付流程，面向 macOS DMG 与 Windows NSIS EXE。

### 变更

- 词库页将常驻导入引导收纳为“Excel 导入帮助”弹窗，保留三步说明、格式预览和模板下载。
- 翻译记忆页增加导入帮助弹窗，并将“导入词条”更正为“导入记忆”，明确记忆句段与术语词条的区别。
- 翻译记忆导入将 Excel、CSV 和 TSV 的文件预览改为原文、译文、备注三列表格，长句与多行备注可直接扫读。
- 翻译记忆导入改为可恢复的后台任务；目标记忆库改为必选，不再自动创建“默认记忆库”。

### 修复

- 统一豆包嵌入与 PostgreSQL 向量列为 2048 维，并使用 halfvec HNSW 检索索引，修复批量导入时报 `expected 1536 dimensions, not 2048`。
- 嵌入数量、维度或数值无效时会在写入文本前失败；向量写入失败会回滚本批文本，避免出现“导入报错但词条已残留”的半成功状态。
- 向量写入改为分批集合更新，避免数千条记忆逐行执行数据库更新造成导入卡顿。
- 缺失向量回填仅处理 `embedding IS NULL` 的记录，并通过持久任务分批续跑。
- 补齐翻译记忆搜索结果、词库编辑、文档翻译、图片翻译和 IDE 操作提示中的中英文文案，避免界面直接显示 i18n 键名。

## [0.6.0] - 2026-07-25

### 新增

- 文档初始化中的可配置分割预览、术语预览与完整术语结果展示。
- 词库导入引导、格式说明和 Excel 模板下载。
- 面向法律与专业翻译的结构化句法质检，覆盖情态、条件与例外、否定辖域、主体角色、并列逻辑、修饰边界、时序、数量、指代和语法语域。
- 人工确认后再生成、应用句法修订的质检工作流。
- 质检基线与修订译文的双版本逐词 Diff；无修改时也明确显示两版一致。
- PDF、DOCX、图片及即时翻译等多种工作区能力。

### 变更

- Dashboard 与 IDE 侧边栏头部统一为“Logo 在前、折叠按钮在后”。
- 术语提取与分割预览采用一致的配置入口和结果查看逻辑。
- 对象存储切换为腾讯云 COS，缓存与任务协调切换为 Valkey，数据库搜索使用 PostgreSQL 与 PGroonga。
- PDF 解析支持 MinerU 在线服务，多模态内容支持独立模型配置。

### 修复

- 已完成初始化的项目不会因重新进入页面而再次回到“解析中”。
- 分段、术语提取和词条应用增加状态比较更新与 Redis 锁，避免重复请求回退状态或破坏已有分段。
- 术语提取任务支持中断后的安全续跑，并能识别已完成或失败的终态。
- 术语嵌入结果按当前分段隔离，避免跨分段显示或保存错误译文。
- 词库页面在加载失败时保留登录状态，并统一使用当前已认证用户身份。
- 修复快捷键事件缺少按键值时调用 `toLowerCase()` 导致的异常。
- 修复邮件登录与注册流程中的重复 SMTP 认证问题。
- 句法质检模型返回缺少字段或必检维度时自动重试一次。
- 生产 Compose 统一使用单一 Traefik 服务与显式 web 网络，避免重建后路由冲突或返回 404。

## [0.1.0] - 2025-11-06

### 新增

- DeepTrans Studio 初始开发版本。
